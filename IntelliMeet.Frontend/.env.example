# Copy to `.env` / `.env.local`. Restart `npm run dev` after changes.
#
# Clerk Dashboard → API Keys → Publishable key (starts with pk_test_ or pk_live_).
# Never add the Clerk "Secret key" here — it belongs only in the backend / server env.

VITE_CLERK_PUBLISHABLE_KEY=
